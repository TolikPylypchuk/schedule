export enum ClassSpreading {
    GROUP,
    DEPARTMENT,
    COURSE
}

export enum DayOfWeek {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY
}

export enum ClassType {
    LECTURE,
    PRACTICE,
    LAB
}
